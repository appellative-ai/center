package operations

// How to startup and support concurrent requests to linked collectives
// We obviously need the registry host names

// The registry would contain the following data:
//   A slice
//
